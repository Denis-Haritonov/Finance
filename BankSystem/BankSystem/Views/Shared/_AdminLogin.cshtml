@if(WebSecurity.IsAuthenticated)
{
    <li class="dropdown">
        <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">
            <i class="halflings-icon white user"></i> @WebSecurity.CurrentUserName
            <span class="caret"></span>
        </a>
        <ul class="dropdown-menu">
            <li><a href="@Url.Action("LogOff","Account")"><i class='halflings-icon off'></i>Выйти</a></li>
        </ul>
    </li>
}